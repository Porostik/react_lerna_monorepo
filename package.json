{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deps": "lerna bootstrap",
    "start": "lerna run start --scope @test/app --stream",
    "build": "lerna run build --scope @test/app --stream",
    "format": "lerna run format --scope @test/configs --stream",
    "lint:all": "lerna run lint:all --scope @test/configs --stream",
    "lint:fix": "eslint --fix 'packages/*/src/**/*.js*'",
    "prepare": "husky install",
    "staged": "lerna run staged --scope @test/configs --stream",
    "install:app": "lerna add --scope @test/app",
    "install:mobile": "lerna add --scope @test/mobile_app",
    "install:shared": "lerna add --scope @test/shared",
    "install:configs": "lerna add --scope @test/configs"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "agent-base": "^6.0.2",
    "babel-jest": "^27.2.1",
    "enzyme": "^3.11.0",
    "enzyme-adapter-react-17-updated": "^1.0.2",
    "enzyme-to-json": "^3.6.2",
    "husky": "^7.0.2",
    "jest": "^27.2.1",
    "lerna": "^4.0.0"
  }
}
